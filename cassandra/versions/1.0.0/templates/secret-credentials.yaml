kind: secret
name: {{ include "cassandra.secret.credentials.name" . }}
type: dictionary
data:
  username: {{ .Values.username }}
  password: {{ .Values.password }}
  keyspace: {{ .Values.keyspaceName }}
