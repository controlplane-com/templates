---
kind: workload
name: {{ include "docmost.redis.name" . }}
description: Docmost Redis — BullMQ queues, socket.io adapter, collab sync
gvc: {{ .Values.global.cpln.gvc }}
tags: {{- include "docmost.tags" . | nindent 4 }}
spec:
  type: stateful
  identityLink: //gvc/{{ .Values.global.cpln.gvc }}/identity/{{ include "docmost.identity.name" . }}
  containers:
    - name: redis
      image: {{ .Values.redis.image }}
      inheritEnv: false
      minCpu: {{ .Values.redis.resources.minCpu | quote }}
      cpu: {{ .Values.redis.resources.maxCpu | quote }}
      minMemory: {{ .Values.redis.resources.minMemory | quote }}
      memory: {{ .Values.redis.resources.maxMemory | quote }}
      command: redis-server
      # AOF + noeviction per the upstream compose — queue entries must never be evicted.
      args:
        - --requirepass
        - $(REDIS_PASSWORD)
        - --appendonly
        - "yes"
        - --maxmemory-policy
        - noeviction
      env:
        - name: REDIS_PASSWORD
          value: cpln://secret/{{ include "docmost.secret.creds.name" . }}.redisPassword
      ports:
        - number: 6379
          protocol: tcp
      readinessProbe:
        exec:
          command:
            - sh
            - -c
            - redis-cli -a "$REDIS_PASSWORD" ping | grep -q PONG
        failureThreshold: 10
        initialDelaySeconds: 10
        periodSeconds: 5
        successThreshold: 1
        timeoutSeconds: 3
      volumes:
        - path: /data
          recoveryPolicy: retain
          uri: cpln://volumeset/{{ include "docmost.redis.volumeset.name" . }}
  defaultOptions:
    autoscaling:
      metric: disabled
      minScale: 1
      maxScale: 1
    capacityAI: false
    debug: false
    suspend: false
  firewallConfig:
    external:
      inboundAllowCIDR: []
      outboundAllowCIDR: []
    internal:
      inboundAllowType: same-gvc
  loadBalancer:
    direct:
      enabled: false
    replicaDirect: false
  supportDynamicTags: false
