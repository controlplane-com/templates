apiVersion: v2
name: duckdb
description: DuckDB scheduled batch SQL job runner — executes a user-supplied SQL script on a cron schedule over Parquet, CSV and JSON in object storage and over attached PostgreSQL, MySQL and SQLite databases, with no standing infrastructure and no server endpoint
type: application
version: 1.0.0
appVersion: "1.5.5"

dependencies:
  - name: cpln-common
    version: 1.0.0
    repository: "oci://ghcr.io/controlplane-com/templates"

annotations:
  created: "2026-08-06"
  lastModified: "2026-08-07"
  category: "analytics"
  createsGvc: false
