kind: secret
name: {{ include "duckdb.secret.preamble.name" . }}
description: DuckDB preamble script (derived settings, run before the job SQL)
type: opaque
tags: {{- include "duckdb.tags" . | nindent 4 }}
data:
  encoding: plain
  payload: |-
    {{- include "duckdb.config.preamble" . | nindent 4 }}
