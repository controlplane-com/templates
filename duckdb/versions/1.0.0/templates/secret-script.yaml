{{- if not .Values.sql.secretName }}
kind: secret
name: {{ include "duckdb.secret.script.name" . }}
description: DuckDB job SQL script
type: opaque
tags: {{- include "duckdb.tags" . | nindent 4 }}
data:
  encoding: plain
  payload: |-
    {{- .Values.sql.inline | nindent 4 }}
{{- end }}
