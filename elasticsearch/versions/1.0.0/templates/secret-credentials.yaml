kind: secret
name: {{ include "elasticsearch.secret.credentials.name" . }}
description: Elasticsearch credentials
tags: {{- include "elasticsearch.tags" . | nindent 2 }}
type: dictionary
data:
  elastic-password: {{ .Values.elasticPassword }}
