image: controlplanecorporation/fusionauth:0.2

resources:
  cpu: 512m
  memory: 1024Mi

firewall:
  external:
    inboundAllowCIDR:
        - 0.0.0.0/0
    outboundAllowCIDR: [] # Set to 0.0.0.0/0 to allow outbound traffic if communicating with IDP
  internal:
    type: same-gvc # options: same-gvc, same-org, workload-list
    workloads:  # Note: can only be used if type is same-gvc or workload-list
      #- //gvc/GVC_NAME/workload/WORKLOAD_NAME
      #- //gvc/GVC_NAME/workload/WORKLOAD_NAME

# Database configuration
postgres:
  image: postgres:18
  # Credentials for the bundled database — internal plumbing no human types elsewhere.
  # FusionAuth CREATES the dictionary secret named by config.credentialsSecretName
  # below out of these three values, so there is nothing for you to create before
  # installing.
  credentials:
    # username and database are UNCHANGED from 2.4.0 on purpose: an existing
    # install's data directory was initialized with them, so a new default would
    # break the upgrade. Only the password placeholder changed — see the README.
    username: username
    password: change-me-fusionauth-db # used EXACTLY as given — change it before installing
    database: test
  config:
    # Name of the dictionary secret this chart creates and the bundled postgres reads.
    # Secret names are org-wide: give each fusionauth release its own name. A second
    # release left on this name is REFUSED at install (it is owned by the first
    # release) — nothing is shared, overwritten or deleted.
    credentialsSecretName: my-fusionauth-db-credentials
  resources:
    minCpu: 200m
    minMemory: 128Mi
    maxCpu: 500m
    maxMemory: 256Mi
  volumeset:
    capacity: 10 # initial capacity in GiB (minimum is 10)
    autoscaling:
      enabled: false # Set to true to enable autoscaling
      maxCapacity: 100 # Maximum capacity in GiB when autoscaling is enabled
      minFreePercentage: 10 # Minimum free percentage to trigger scaling when autoscaling is enabled
      scalingFactor: 1.2 # Scaling factor to determine how much to scale up when autoscaling is triggered

  internalAccess: # Sets the internal firewall scope - if set to none, replicas will not be able to reach each other
    type: same-gvc # options: none, same-gvc, same-org, workload-list
    workloads:  # Note: can only be used if type is same-gvc or workload-list
      #- //gvc/GVC_NAME/workload/WORKLOAD_NAME
      #- //gvc/GVC_NAME/workload/WORKLOAD_NAME

  # Postgres backup configuration   
  backup: # compatible with Postgres 17+
    enabled: false
    image: ghcr.io/controlplane-com/backup-images/postgres-backup:18.1.0 # tag 18.1.0 = Postgres 18, 17.1.0 = Postgres 17
    schedule: "0 2 * * *"   # daily at 2am UTC

    resources:
      cpu: 100m
      memory: 128Mi

    provider: aws # Options: aws or gcp

    aws:
      bucket: my-backup-bucket
      region: us-east-1
      cloudAccountName: my-backup-cloudaccount
      policyName: my-backup-policy
      prefix: postgres/backups # folder name where your backups will be stored

    gcp:
      bucket: my-backup-bucket
      cloudAccountName: my-backup-cloudaccount
      prefix: postgres/backups # folder name where your backups will be stored