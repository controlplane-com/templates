kind: identity
name: {{ include "glitchtip.identity.name" . }}
description: GlitchTip workload identity (shared by web and worker)
gvc: {{ .Values.global.cpln.gvc }}
tags: {{- include "glitchtip.tags" . | nindent 4 }}
