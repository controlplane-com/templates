{{- if .Values.backup.enabled }}
kind: workload
name: {{ include "maria.backup.name" . }}
description: MariaDB Backup
tags:
  {{- include "maria.tags" . | nindent 2 }}
spec:
  type: cron
  containers:
    - name: backup-mariadb
      cpu: {{ .Values.backup.resources.cpu | quote }}
      memory: {{ .Values.backup.resources.memory | quote }}
      env:
      {{- if eq .Values.backup.provider "aws" }}
        - name: AWS_REGION
          value: {{ .Values.backup.aws.region | quote }}
        - name: BACKUP_PROVIDER
          value: aws
        - name: BACKUP_BUCKET
          value: {{ .Values.backup.aws.bucket | quote }}
        - name: BACKUP_PREFIX
          value: {{ .Values.backup.aws.prefix | quote }}
      {{- end }}
      {{- if eq .Values.backup.provider "gcp" }}
        - name: BACKUP_PROVIDER
          value: gcp
        - name: BACKUP_BUCKET
          value: {{ .Values.backup.gcp.bucket | quote }}
        - name: BACKUP_PREFIX
          value: {{ .Values.backup.gcp.prefix | quote }}
      {{- end }}
        - name: MYSQL_DATABASE
          value: 'cpln://secret/{{ .Values.credentialsSecretName }}.database'
        - name: MYSQL_HOST
          value: {{ include "maria.name" . }}.{{ .Values.global.cpln.gvc }}.cpln.local
        - name: MYSQL_ROOT_PASSWORD
          value: 'cpln://secret/{{ .Values.rootPasswordSecretName }}.payload'
        - name: MYSQL_PASSWORD
          value: 'cpln://secret/{{ .Values.credentialsSecretName }}.password'
        - name: MYSQL_PORT
          value: '3306'
        - name: MYSQL_USER
          value: 'cpln://secret/{{ .Values.credentialsSecretName }}.username'
      image: {{ .Values.backup.image }}
      inheritEnv: false
  defaultOptions:
    autoscaling:
      maxConcurrency: 0
      maxScale: 1
      metric: disabled
      minScale: 1
      scaleToZeroDelay: 300
      target: 95
    capacityAI: false
    debug: false
    suspend: false
    timeoutSeconds: 5
  firewallConfig:
    external:
      inboundAllowCIDR: []
      inboundBlockedCIDR: []
      outboundAllowCIDR:
        - 0.0.0.0/0
      outboundAllowHostname: []
      outboundAllowPort: []
      outboundBlockedCIDR: []
    internal:
      inboundAllowType: none
      inboundAllowWorkload: []
  identityLink: //identity/{{ include "maria.identity.name" . }}
  job:
    concurrencyPolicy: Forbid
    historyLimit: 5
    restartPolicy: Never
    schedule: {{ .Values.backup.schedule | quote }}
  supportDynamicTags: false
{{- end }}
