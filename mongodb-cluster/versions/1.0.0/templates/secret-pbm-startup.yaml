{{- if and .Values.backup.enabled (eq .Values.backup.mode "physical") }}
---
kind: secret
name: {{ include "mongo-cluster.secretPbmStartup.name" . }}
description: PBM agent startup script
tags: {{- include "mongo-cluster.tags" . | nindent 2 }}
type: opaque
data:
  encoding: plain
  payload: |-
    #!/usr/bin/env sh
    set -eu

    MONGO_URI="mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@localhost:27017/admin?replicaSet=rs0&authSource=admin"

    {{- if eq .Values.backup.provider "aws" }}
    cat > /tmp/pbm-config.yaml <<EOF
    storage:
      type: s3
      s3:
        region: ${AWS_REGION}
        bucket: ${PBM_BUCKET}
        prefix: ${PBM_PREFIX}
    EOF
    {{- end }}
    {{- if eq .Values.backup.provider "gcp" }}
    cat > /tmp/pbm-config.yaml <<EOF
    storage:
      type: gcs
      gcs:
        bucket: ${PBM_BUCKET}
        prefix: ${PBM_PREFIX}
    EOF
    {{- end }}

    echo "Applying PBM storage configuration..."
    until pbm config --mongodb-uri="${MONGO_URI}" --file /tmp/pbm-config.yaml; do
      echo "Waiting for MongoDB to be ready, retrying in 10s..."
      sleep 10
    done

    echo "Setting backup schedule: ${BACKUP_SCHEDULE}"
    pbm schedule delete --all --mongodb-uri="${MONGO_URI}" 2>/dev/null || true
    pbm schedule add --cron="${BACKUP_SCHEDULE}" --type=physical --mongodb-uri="${MONGO_URI}"

    echo "Starting pbm-agent..."
    exec pbm-agent --mongodb-uri="${MONGO_URI}"
{{- end }}
