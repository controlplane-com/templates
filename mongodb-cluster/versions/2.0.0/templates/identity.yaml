{{- /*
The single unconditional entry point for validation — "is validation still wired
up?" is one grep. Every template that iterates `locations` includes it again,
because Helm does not render templates in a guaranteed order.
*/ -}}
{{- include "mongo-cluster.validate" . -}}
---
kind: identity
gvc: {{ .Values.global.cpln.gvc }}
name: {{ include "mongo-cluster.identity.name" . }}
description: MongoDB identity
tags: {{- include "mongo-cluster.tags" . | nindent 2 }}
{{- if and .Values.backup.enabled (eq .Values.backup.provider "aws") }}
aws:
  cloudAccountLink: //cloudaccount/{{ .Values.backup.aws.cloudAccountName }}
  policyRefs:
    - cpln-connector
    - {{ .Values.backup.aws.policyName | quote }}
{{- end }}
{{- if and .Values.backup.enabled (eq .Values.backup.provider "gcp") }}
gcp:
  bindings:
    - resource: //storage.googleapis.com/projects/_/buckets/{{ .Values.backup.gcp.bucket }}
      roles:
        - roles/storage.objectAdmin
  cloudAccountLink: //cloudaccount/{{ .Values.backup.gcp.cloudAccountName }}
  scopes:
    - https://www.googleapis.com/auth/cloud-platform
{{- end }}
