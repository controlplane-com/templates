{{- include "mongo.validateBackupConfig" . }}
{{- if .Values.backup.enabled }}
kind: workload
name: {{ include "mongo.backup.name" . }}
description: MongoDB Backup
tags:
  {{- include "mongo.tags" . | nindent 2 }}
spec:
  type: cron
  containers:
    - name: backup-mongodb
      cpu: {{ .Values.backup.resources.cpu | quote }}
      memory: {{ .Values.backup.resources.memory | quote }}
      env:
      {{- if eq .Values.backup.provider "aws" }}
        - name: AWS_REGION
          value: cpln://secret/{{ include "mongo.secretDatabase.name" . }}.aws-region
        - name: BACKUP_PROVIDER
          value: aws
        - name: BACKUP_BUCKET
          value: cpln://secret/{{ include "mongo.secretDatabase.name" . }}.backup-bucket
        - name: BACKUP_PREFIX
          value: {{ .Values.backup.aws.prefix | quote }}
      {{- end }}
      {{- if eq .Values.backup.provider "gcp" }}
        - name: BACKUP_PROVIDER
          value: gcp
        - name: BACKUP_BUCKET
          value: cpln://secret/{{ include "mongo.secretDatabase.name" . }}.backup-bucket
        - name: BACKUP_PREFIX
          value: {{ .Values.backup.gcp.prefix | quote }}
      {{- end }}
        - name: MONGO_HOST
          value: {{ include "mongo.name" . }}.{{ .Values.global.cpln.gvc }}.cpln.local
        - name: MONGO_USER
          value: cpln://secret/{{ include "mongo.secretDatabase.name" . }}.username
        - name: MONGO_PASSWORD
          value: cpln://secret/{{ include "mongo.secretDatabase.name" . }}.password
      image: {{ .Values.backup.image }}
      inheritEnv: false
  defaultOptions:
    autoscaling:
      maxConcurrency: 0
      maxScale: 1
      metric: disabled
      minScale: 1
      scaleToZeroDelay: 300
      target: 95
    capacityAI: false
    debug: false
    suspend: false
    timeoutSeconds: 5
  firewallConfig:
    external:
      inboundAllowCIDR: []
      inboundBlockedCIDR: []
      outboundAllowCIDR:
        - 0.0.0.0/0
      outboundAllowHostname: []
      outboundAllowPort: []
      outboundBlockedCIDR: []
    internal:
      inboundAllowType: none
      inboundAllowWorkload: []
  identityLink: //identity/{{ include "mongo.identity.name" . }}
  job:
    concurrencyPolicy: Forbid
    historyLimit: 5
    restartPolicy: Never
    schedule: {{ .Values.backup.schedule }}
  supportDynamicTags: false
{{- end }}
