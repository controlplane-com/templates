kind: volumeset
name: {{ include "open-webui.volume.name" . }}
gvc: {{ .Values.global.cpln.gvc }}
description: Open WebUI data directory (SQLite database, uploads, RAG vector store, cache)
tags: {{- include "open-webui.tags" . | nindent 4 }}
spec:
  fileSystemType: ext4
  initialCapacity: {{ .Values.volumeset.capacity }}
  performanceClass: general-purpose-ssd
  snapshots:
    createFinalSnapshot: true
    retentionDuration: {{ .Values.backup.retention | quote }}
    {{- if .Values.backup.enabled }}
    schedule: {{ .Values.backup.schedule | quote }}
    {{- end }}
