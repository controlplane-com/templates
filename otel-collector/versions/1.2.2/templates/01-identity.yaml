kind: identity
name: {{ include "oc.name" . }}
description: Identity for the OpenTelemetry Collector — grants reveal on its config and auth secrets
tags: {{- include "oc.tags" . | nindent 4 }}
gvc: {{ .Values.global.cpln.gvc }}
