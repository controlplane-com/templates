kind: secret
name: {{ include "polaris.secret.bootstrapScript.name" . }}
description: Polaris bootstrap script — runs the admin tool until it succeeds, then idles
type: opaque
tags: {{- include "polaris.tags" . | nindent 4 }}
data:
  encoding: plain
  payload: |-
    {{- include "polaris.bootstrap.script" . | nindent 4 }}
