kind: secret
name: {{ include "prometheus.secret.config.name" . }}
type: opaque
tags: {{- include "prometheus.tags" . | nindent 4 }}
data:
  encoding: plain
  payload: |-
    {{- include "prometheus.config" . | nindent 4 }}
