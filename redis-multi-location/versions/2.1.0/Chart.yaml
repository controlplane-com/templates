apiVersion: v2
name: redis-multi-location
description: One Redis master-replica cluster stretched across locations, with Redis Sentinel electing a new master when a location is lost, optional public TCP access and optional backups to object storage
type: application
version: 2.1.0
appVersion: "7.4"

annotations:
  created: "2025-12-13"
  lastModified: "2026-08-12"
  category: "cache"
  createsGvc: true

dependencies:
  - name: cpln-common
    version: 1.0.0
    repository: "oci://ghcr.io/controlplane-com/templates"
