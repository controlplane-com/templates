{{- if .Values.backup.enabled }}
kind: workload
name: {{ include "timescaledb.backup.name" . }}
description: TimescaleDB Backup
tags: {{- include "timescaledb.tags" . | nindent 4 }}
spec:
  type: cron
  containers:
    - name: backup-timescaledb
      cpu: {{ .Values.backup.resources.cpu | quote }}
      memory: {{ .Values.backup.resources.memory | quote }}
      env:
      {{- if eq .Values.backup.provider "aws" }}
        - name: AWS_REGION
          value: cpln://secret/{{ include "timescaledb.secretDatabase.name" . }}.aws-region
        - name: BACKUP_PROVIDER
          value: aws
        - name: BACKUP_BUCKET
          value: cpln://secret/{{ include "timescaledb.secretDatabase.name" . }}.backup-bucket
        - name: BACKUP_PREFIX
          value: {{ .Values.backup.aws.prefix | quote }}
      {{- end }}
      {{- if eq .Values.backup.provider "gcp" }}
        - name: BACKUP_PROVIDER
          value: gcp
        - name: BACKUP_BUCKET
          value: cpln://secret/{{ include "timescaledb.secretDatabase.name" . }}.backup-bucket
        - name: BACKUP_PREFIX
          value: {{ .Values.backup.gcp.prefix | quote }}
      {{- end }}
      {{- if eq .Values.backup.provider "minio" }}
        - name: BACKUP_PROVIDER
          value: minio
        - name: BACKUP_BUCKET
          value: cpln://secret/{{ include "timescaledb.secretDatabase.name" . }}.backup-bucket
        - name: BACKUP_PREFIX
          value: {{ .Values.backup.minio.prefix | quote }}
        - name: MINIO_ENDPOINT
          value: cpln://secret/{{ include "timescaledb.secretDatabase.name" . }}.minio-endpoint
        - name: MINIO_ACCESS_KEY
          value: cpln://secret/{{ include "timescaledb.secretDatabase.name" . }}.minio-access-key
        - name: MINIO_SECRET_KEY
          value: cpln://secret/{{ include "timescaledb.secretDatabase.name" . }}.minio-secret-key
      {{- end }}
        - name: PG_HOST
          value: {{ include "timescaledb.name" . }}.{{ .Values.global.cpln.gvc }}.cpln.local
        - name: PG_PASSWORD
          value: cpln://secret/{{ include "timescaledb.secretDatabase.name" . }}.password
        - name: PG_PORT
          value: '5432'
        - name: PG_USER
          value: cpln://secret/{{ include "timescaledb.secretDatabase.name" . }}.username
      image: {{ .Values.backup.image }}
      inheritEnv: false
  defaultOptions:
    autoscaling:
      maxConcurrency: 0
      maxScale: 1
      metric: disabled
      minScale: 1
      scaleToZeroDelay: 300
      target: 95
    capacityAI: false
    debug: false
    suspend: false
    timeoutSeconds: 5
  firewallConfig:
    external:
      inboundAllowCIDR: []
      inboundBlockedCIDR: []
      outboundAllowCIDR:
        - 0.0.0.0/0
      outboundAllowHostname: []
      outboundAllowPort: []
      outboundBlockedCIDR: []
    internal:
      inboundAllowType: none
      inboundAllowWorkload: []
  identityLink: //identity/{{ include "timescaledb.identity.name" . }}
  job:
    concurrencyPolicy: Forbid
    historyLimit: 5
    restartPolicy: Never
    schedule: {{ .Values.backup.schedule }}
  supportDynamicTags: false
{{- end }}
