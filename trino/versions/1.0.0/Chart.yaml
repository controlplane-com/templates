apiVersion: v2
name: trino
description: Trino distributed SQL query engine — a coordinator plus a scalable worker tier that runs federated queries across PostgreSQL, MySQL, ClickHouse, MongoDB and other data sources without moving the data
type: application
version: 1.0.0
appVersion: "483"

dependencies:
  - name: cpln-common
    version: 1.0.0
    repository: "oci://ghcr.io/controlplane-com/templates"

annotations:
  created: "2026-08-05"
  lastModified: "2026-08-05"
  category: "analytics"
  createsGvc: false
