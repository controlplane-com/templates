---
kind: volumeset
name: {{ include "twenty.redis.volumeset.name" . }}
description: Twenty Redis AOF persistence
tags: {{- include "twenty.tags" . | nindent 4 }}
gvc: {{ .Values.global.cpln.gvc }}
spec:
  fileSystemType: ext4
  initialCapacity: {{ .Values.redis.volumeset.capacity }}
  performanceClass: general-purpose-ssd
