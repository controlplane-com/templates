kind: secret
name: {{ include "unleash.secretAdmin.name" . }}
description: Unleash default admin bootstrap credentials (seeded on first boot only)
tags: {{- include "unleash.tags" . | nindent 4 }}
type: dictionary
data:
  username: {{ .Values.admin.username | quote }}
  password: {{ .Values.admin.password | quote }}
